set(WRPARSE_ROOT "@CMAKE_INSTALL_PREFIX@")

set(WRPARSE_VERSION_MAJOR @WRPARSE_VERSION_MAJOR@)
set(WRPARSE_VERSION_MINOR @WRPARSE_VERSION_MINOR@)
set(WRPARSE_VERSION_PATCH @WRPARSE_VERSION_PATCH@)

include_directories(${WRPARSE_ROOT}/include @PCRE2_INCLUDE@)
link_directories(${WRPARSE_ROOT}/lib @PCRE2_LIB@)

# for check_cxx_*() functions
list(APPEND CMAKE_REQUIRED_INCLUDES ${WRPARSE_ROOT}/include)
